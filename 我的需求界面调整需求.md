MVP 版本：会话框吸底的需求描述
核心功能
创建一个底部吸附的 AI 会话输入框，配合全屏编辑器使用

布局结构
┌────────────────────────────────────────────────────────────┐
│ 顶部信息栏 - 56px                                          │
│ 需求名称 [输入框] 归属项目 [下拉框]                        │
├────────────────────────────────────────────────────────────┤
│                                                             │
│ 编辑器区域 - 自适应高度（占满剩余空间）                    │
│                                                             │
│ ┌──────────────────────────────────────────────────────┐  │
│ │ B I U H1 H2 • 1. [] 🔗                                │  │
│ ├──────────────────────────────────────────────────────┤  │
│ │                                                       │  │
│ │ # 需求背景                                            │  │
│ │                                                       │  │
│ │ [编辑内容，可滚动]                                    │  │
│ │                                                       │  │
│ │                                                       │  │
│ └───────────────────────────────────────────────────────┘  │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│ 底部吸附操作栏 - 固定 160px                                │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 向 AI 提问...                                           │ │
│ │ ┌───────────────────────────────────────────────────┐   │ │
│ │ │ [输入框 - 最小高度 60px，最大 120px]              │   │ │
│ │ └───────────────────────────────────────────────────┘   │ │
│ │                                                         │ │
│ │ [📎 上传参考图] [🎯 P1▼] [🤖 GPT-4▼]        [↑ 发送] │ │
│ └─────────────────────────────────────────────────────────┘ │
│ [💾 保存草稿]                                  [下一步 >]  │
└─────────────────────────────────────────────────────────────┘

详细规格
1. 顶部信息栏（56px）
HTML 结构：
html<div class="top-bar">
  <input placeholder="需求名称 *" />
  <select>
    <option>外呼平台</option>
  </select>
</div>
CSS 样式：

高度：56px
背景：#FFFFFF
底部边框：1px solid #E5E7EB
内边距：0 24px
布局：flex，左右分布
输入框宽度：60%
下拉框宽度：35%


2. 编辑器区域（自适应）
高度计算：
cssheight: calc(100vh - 56px - 160px - 环境UI预留);
/* 约等于 600-700px (1080p显示器) */
工具栏（48px）：

背景：#F9FAFB
底部边框：1px solid #E5E7EB
按钮尺寸：32×32px
按钮间距：4px
悬停背景：#E5E7EB

编辑区：

内边距：24px
字体：14px/1.6
颜色：#1F2937
滚动：auto（超出显示滚动条）
背景：#FFFFFF


3. 底部吸附操作栏（固定 160px）
3.1 AI 输入区域（120px）
HTML 结构：
html<div class="bottom-panel">
  <div class="ai-input-container">
    <textarea 
      placeholder="向 AI 提问..."
      rows="2"
    ></textarea>
    
    <div class="input-actions">
      <button class="upload-btn">📎 上传参考图</button>
      <select class="priority">
        <option>🎯 P1-重要</option>
      </select>
      <select class="model">
        <option>🤖 GPT-4</option>
      </select>
      <button class="send-btn">↑ 发送</button>
    </div>
  </div>
</div>
CSS 样式：
css.bottom-panel {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 160px;
  background: #FFFFFF;
  box-shadow: 0 -2px 8px rgba(0, 0, 0, 0.08);
  border-top: 1px solid #E5E7EB;
  z-index: 100;
}

.ai-input-container {
  padding: 16px 24px;
  height: 120px;
}

.ai-input-container textarea {
  width: 100%;
  min-height: 60px;
  max-height: 120px;
  padding: 12px 16px;
  border: 1px solid #D1D5DB;
  border-radius: 8px;
  font-size: 14px;
  resize: vertical;
}

.ai-input-container textarea:focus {
  outline: none;
  border-color: #3B82F6;
  box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
}

.input-actions {
  display: flex;
  gap: 12px;
  margin-top: 12px;
  justify-content: space-between;
  align-items: center;
}

.upload-btn, .priority, .model {
  height: 36px;
  padding: 0 16px;
  border: 1px solid #D1D5DB;
  border-radius: 6px;
  background: #FFFFFF;
  font-size: 14px;
  cursor: pointer;
}

.send-btn {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background: #3B82F6;
  color: white;
  border: none;
  font-size: 18px;
  cursor: pointer;
  flex-shrink: 0;
}

.send-btn:hover {
  background: #2563EB;
}
3.2 主操作按钮区（40px）
HTML 结构：
html<div class="main-actions">
  <button class="save-draft">💾 保存草稿</button>
  <button class="next-step">下一步 ></button>
</div>
CSS 样式：
css.main-actions {
  height: 40px;
  padding: 0 24px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-top: 1px solid #E5E7EB;
}

.save-draft {
  padding: 0 20px;
  height: 36px;
  border: 1px solid #D1D5DB;
  border-radius: 6px;
  background: #FFFFFF;
  font-size: 14px;
  cursor: pointer;
}

.next-step {
  padding: 0 24px;
  height: 36px;
  background: #3B82F6;
  color: white;
  border: none;
  border-radius: 6px;
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
}

.next-step:hover {
  background: #2563EB;
}

核心交互
1. 输入框自动扩展
javascriptconst textarea = document.querySelector('.ai-input-container textarea');

textarea.addEventListener('input', function() {
  this.style.height = 'auto';
  this.style.height = Math.min(this.scrollHeight, 120) + 'px';
});
2. 发送消息
javascriptdocument.querySelector('.send-btn').addEventListener('click', function() {
  const message = textarea.value.trim();
  if (!message) return;
  
  // 发送到 AI
  sendToAI(message);
  
  // 清空输入框
  textarea.value = '';
  textarea.style.height = '60px';
});
3. AI 回复展示
javascriptfunction showAIResponse(response) {
  // 在输入框上方插入回复区域
  const replyBox = document.createElement('div');
  replyBox.className = 'ai-reply';
  replyBox.innerHTML = `
    <div class="reply-header">
      🤖 AI 回复
      <button class="close-btn">×</button>
    </div>
    <div class="reply-content">${response}</div>
    <div class="reply-actions">
      <button class="insert-btn">插入到编辑器</button>
      <button class="copy-btn">复制</button>
    </div>
  `;
  
  document.querySelector('.ai-input-container').prepend(replyBox);
}
AI 回复区样式：
css.ai-reply {
  margin-bottom: 16px;
  padding: 16px;
  background: #F9FAFB;
  border: 1px solid #E5E7EB;
  border-radius: 8px;
  max-height: 300px;
  overflow-y: auto;
}

.reply-header {
  display: flex;
  justify-content: space-between;
  font-weight: 500;
  margin-bottom: 12px;
}

.reply-content {
  font-size: 14px;
  line-height: 1.6;
  color: #374151;
  margin-bottom: 12px;
}

.reply-actions {
  display: flex;
  gap: 8px;
}

.reply-actions button {
  padding: 6px 12px;
  border: 1px solid #D1D5DB;
  border-radius: 4px;
  background: white;
  font-size: 13px;
  cursor: pointer;
}
```

---

## 完整页面高度分配

**1080p 显示器（可视区域 约 900px）：**
```
顶部栏：      56px
编辑器：     684px  (900 - 56 - 160)
底部操作栏：  160px
──────────────────
总计：       900px ✓ 完美适配首屏
优势：

✅ 编辑区占比 76%（684/900）
✅ 所有核心操作在首屏
✅ 无需滚动即可完成基本操作


MVP 功能清单
必须实现

 顶部需求名称输入
 项目归属选择
 富文本编辑器（支持 Markdown）
 底部吸附 AI 输入框
 输入框高度自适应（60-120px）
 上传参考图按钮
 优先级选择
 AI 模型选择
 发送按钮
 保存草稿按钮
 下一步按钮